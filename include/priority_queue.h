// ------------------------- HEAP SORT ---------------------------- #
// In-Place: requires O(1) of auxiliary memory space.
// UnStable: may change the relative order of elements with equal keys.
// Online: can sort a list as it receives it.
//
// Runtime T(n):
//   Best: T(n) ~ n * log(n)  (if all keys are distinct)
//   Best: T(n) ~ n
//   Average: T(n) ~ 2 * n * log(n)
//   Worst: T(n) ~ 2 * n * log(n)
//
// Asymptotically Analysis (Big O):
//   Best: T(n) = O(n * log(n))  (if all keys are distinct)
//   Best: T(n) = O(n)
//   Average: T(n) = O(n * log(n))
//   Worst: T(n) = O(n * log(n))
//
// ----------------------------------------------------------------------- #
// TODO: IMPLEMENT
